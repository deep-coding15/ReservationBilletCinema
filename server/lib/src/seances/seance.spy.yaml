class: Seance
table: seances
managedMigration: true
fields:
  film_id: int
  salle_id: int
  date_heure: DateTime
  langue: String, default='VF'
  type_projection: String, default='2D'
  places_disponibles: int
  prix: double
  type_seance: String, default='standard'
  created_at: DateTime, default=now
indexes:
  idx_seances_film:
    fields: film_id
  idx_seances_salle:
    fields: salle_id
  idx_seances_date:
    fields: date_heure